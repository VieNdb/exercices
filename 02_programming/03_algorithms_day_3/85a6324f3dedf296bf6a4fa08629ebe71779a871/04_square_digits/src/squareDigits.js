function squareDigits(valueOfNumber, tableOfValue) {
  // Code here
for (let i = 0; i < tableOfValue.length; i ++) {
  return valueOfNumber * valueOfNumber ;
}
}

// Leave the line below for tests to work
module.exports = squareDigits;
