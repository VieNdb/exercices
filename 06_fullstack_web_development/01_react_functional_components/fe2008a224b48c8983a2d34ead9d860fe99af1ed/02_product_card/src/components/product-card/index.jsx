import React from "react";
import CardHeader from "./header";

const ProductCard = () => {
  return (
    <>
    <CardHeader name={product.name} platformLogos={zelda.platforms[0].platform_logo.url}  />
    </>
  );
};

export default ProductCard;
